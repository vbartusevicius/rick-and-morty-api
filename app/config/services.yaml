imports:
  - { resource: 'services/' }

parameters:

services:
  _defaults:
    autowire: true
    autoconfigure: true

  App\:
    resource: '../src/'
    exclude:
      - '../src/DependencyInjection/'
      - '../src/Entity/'
      - '../src/Kernel.php'

  App\GraphQL\Resolver\Users:
    tags:
      - { name: overblog_graphql.query}

